# Optional. Required only for server persistence.
DATABASE_URL=

# Required for OCR (camera/upload extraction on machine intake fields).
# Get a key at https://aistudio.google.com/app/apikey
# NEVER prefix this with NEXT_PUBLIC_ — server-side only.
GEMINI_API_KEY=
